\c library;

CREATE TABLE public.book_inventory (
                                       id bigint NOT NULL,
                                       book_id bigint NOT NULL,
                                       borrowed_at timestamp(6) without time zone,
                                       return_by timestamp(6) without time zone
);

ALTER TABLE public.book_inventory OWNER TO api;

ALTER TABLE public.book_inventory ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.book_inventory_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

ALTER TABLE ONLY public.book_inventory
    ADD CONSTRAINT book_inventory_pkey PRIMARY KEY (id);

ALTER TABLE ONLY public.book_inventory
    ADD CONSTRAINT ukl6mweh11cb2wo34yqtw09p4q UNIQUE (book_id);
